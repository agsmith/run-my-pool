[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --cov=.
    --cov-report=html
    --cov-report=term-missing
    --cov-report=xml
    --cov-fail-under=70
    --cov-config=.coveragerc
    --strict-markers
    --disable-warnings
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::UserWarning
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
    unit: marks tests as unit tests
    scenario: marks tests as end-to-end season scenario tests
    security: marks tests as OWASP security tests
    season: marks tests as full-season simulations
    gap: marks tests that document a known enforcement gap in the codebase
    known_bug: marks tests that document a known security bug in the codebase
    owasp: marks tests mapped to the OWASP Top 10
