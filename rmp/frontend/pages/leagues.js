export default function Leagues() {
  return (
    <main>
      <h1>Leagues</h1>
      <p>Browse, join, or create leagues. (Public and private leagues supported.)</p>
      {/* TODO: List leagues, join/create forms, search/filter, etc. */}
    </main>
  );
}
